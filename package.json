{
  "name": "mintstart",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "description": "薄荷起始页 - 可定制的新标签页，支持账户与云端同步",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "deploy:pages": "npm run build && wrangler pages deploy dist --project-name mintstart --branch main --commit-dirty=true"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.2.2",
    "wrangler": "^4.129.0"
  }
}
