# 2026-09-03

## 起始页时钟/搜索组件修复（src/styles.css, ClockSearch.jsx 未动）
- 修复悬停死循环：根因是 `.clock` 按钮命中区溢出 `.clock-shell`（按钮 98px > shell 92px，且 `::before` 外壳 inset -7px -15px 扩大命中区），展开搜索后指针下无可悬停元素 → 反复展开/关闭。修法：删掉 `::before`、按钮 padding 改为 4px 10px 8px 使其不溢出 shell、`::after` 加 pointer-events:none。
- 去掉时间悬停玻璃遮罩（删除 `.clock::before` 及 hover 规则），保留下划线动效。
- 时间样式：letter-spacing -.075em → .045em，字重 280 → 300；shell 未展开宽度 236 → 268px 防止加宽字距后文本溢出。
- 构建通过；预览端口 5199。
